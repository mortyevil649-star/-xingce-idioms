import { BookOpen,House,Shuffle } from 'lucide-react'
import { NavLink,Outlet } from 'react-router-dom'
const nav=[['/','首页',House],['/idioms','成语库',BookOpen],['/quiz','随机抽查',Shuffle]] as const
export function Layout(){return <div className="min-h-screen"><header className="sticky top-0 z-20 border-b border-indigo-100 bg-white/90 backdrop-blur"><div className="mx-auto flex max-w-6xl items-center justify-between px-4 py-3"><NavLink to="/" className="display text-xl font-bold tracking-wider text-indigo-950">行测成语积累</NavLink><nav className="flex gap-1">{nav.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'} className={({isActive})=>`focus-ring flex items-center gap-1.5 rounded-lg px-3 py-2 text-sm font-bold ${isActive?'bg-indigo-50 text-indigo-800':'text-slate-500 hover:text-indigo-700'}`}><Icon size={16}/><span className="hidden sm:inline">{label}</span></NavLink>)}</nav></div></header><main className="mx-auto max-w-6xl px-4 py-7 sm:py-10"><Outlet/></main><footer className="mx-auto max-w-6xl px-4 py-8 text-center text-xs text-slate-400">行测成语积累 · 学得慢一点，记得牢一点</footer></div>}
