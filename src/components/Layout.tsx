import { BookOpen,House,Shuffle } from 'lucide-react'
import { NavLink,Outlet } from 'react-router-dom'
const nav=[['/','首页',House],['/idioms','成语库',BookOpen],['/quiz','随机抽查',Shuffle]] as const
export function Layout(){return <div className="min-h-screen"><header className="sticky top-0 z-20 border-b border-indigo-100 bg-white/90 backdrop-blur"><div className="mx-auto flex max-w-6xl items-center justify-between gap-2 px-3 py-2 sm:px-4 sm:py-3"><NavLink to="/" className="display min-w-0 truncate text-base font-bold tracking-wide text-indigo-950 sm:text-xl sm:tracking-wider">行测成语积累</NavLink><nav className="flex shrink-0 gap-1">{nav.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'} className={({isActive})=>`focus-ring flex min-h-11 min-w-11 items-center justify-center gap-1.5 rounded-lg px-2 py-2 text-sm font-bold sm:px-3 ${isActive?'bg-indigo-50 text-indigo-800':'text-slate-500 hover:text-indigo-700'}`}><Icon size={17}/><span className="hidden sm:inline">{label}</span></NavLink>)}</nav></div></header><main className="mx-auto w-full max-w-6xl px-3 py-5 sm:px-4 sm:py-10"><Outlet/></main><footer className="mx-auto max-w-6xl px-3 py-7 text-center text-xs text-slate-400 sm:px-4 sm:py-8">行测成语积累 · 学得慢一点，记得牢一点</footer></div>}
