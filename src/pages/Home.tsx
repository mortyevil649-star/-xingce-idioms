import {ArrowRight,BookOpen,Shuffle,TriangleAlert,type LucideIcon} from 'lucide-react'
import {Link} from 'react-router-dom'
import {useEffect,useMemo,useState} from 'react'
import {examConfig} from '../config/exam'
import {idioms} from '../data/idioms'
import {getRecords} from '../store'
import {Pronunciation} from '../components/Pronunciation'
export function Home(){const [,setTick]=useState(0);useEffect(()=>{const t=setInterval(()=>setTick(x=>x+1),60000);return()=>clearInterval(t)},[]);const days=Math.max(0,Math.ceil((new Date(examConfig.targetDate).getTime()-Date.now())/86400000));const [version,setVersion]=useState(0);useEffect(()=>{const f=()=>setVersion(x=>x+1);window.addEventListener('idiom-progress',f);return()=>window.removeEventListener('idiom-progress',f)},[]);const records=useMemo(()=>getRecords(),[version]);const mastered=Object.values(records).filter(x=>x.status==='mastered').length,mistakes=Object.values(records).filter(x=>x.status==='mistake').length;const random=useMemo(()=>idioms[Math.floor(Math.random()*idioms.length)],[]);const stats:{value:number;label:string;icon:LucideIcon;color:string}[]=[{value:idioms.length,label:'成语总数',icon:BookOpen,color:'text-indigo-700'},{value:mastered,label:'已掌握',icon:BookOpen,color:'text-emerald-600'},{value:mistakes,label:'易错',icon:TriangleAlert,color:'text-rose-600'}];return <div><section className="grid gap-5 lg:grid-cols-[1.35fr_.65fr]"><div className="overflow-hidden rounded-3xl bg-indigo-950 p-7 text-white sm:p-10"><p className="text-sm font-bold tracking-[.2em] text-indigo-300">每天积累一点</p><h1 className="display mt-5 text-4xl font-bold leading-tight sm:text-6xl">把容易混淆的词，<br/><span className="text-amber-300">真正分清楚。</span></h1><p className="mt-6 max-w-xl leading-7 text-indigo-200">专注行测高频易错成语。记释义，更要记使用对象、感情色彩和常见陷阱。</p><div className="mt-8 flex flex-wrap gap-3"><Link className="btn bg-white text-indigo-950 hover:bg-indigo-50" to="/idioms">开始学习 <ArrowRight size={18}/></Link><Link className="btn border border-indigo-700 text-white hover:bg-indigo-900" to="/quiz"><Shuffle size={17}/>随机抽查</Link></div></div><div className="paper flex min-h-64 flex-col justify-between overflow-hidden rounded-3xl p-7"><div><p className="text-sm font-bold text-indigo-600">{examConfig.title}</p><div className="mt-5 flex min-w-0 items-end gap-2"><strong className="countdown-number text-indigo-950">{days}</strong><span className="countdown-unit shrink-0 text-slate-600">天</span></div></div><div className="border-t border-slate-100 pt-4 text-sm text-slate-500"><p>目标日期 · {examConfig.displayDate}</p><p className="mt-1 text-xs">{examConfig.timezone} 时区</p></div></div></section><section className="mt-5 grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><div className="paper rounded-2xl p-6"><p className="text-xs font-bold tracking-widest text-amber-600">今日学习提示</p><p className="display mt-3 text-xl font-bold leading-8 text-slate-800">先辨感情色彩，再看适用对象。遇到“看起来熟悉”的词，更要停一秒。</p></div><div className="grid grid-cols-3 gap-3">{stats.map(({value,label,icon:Icon,color})=><div key={label} className="paper rounded-2xl p-4 sm:p-6"><Icon className={color} size={20}/><strong className="mt-5 block text-2xl sm:text-3xl">{value}</strong><span className="text-xs text-slate-500 sm:text-sm">{label}</span></div>)}</div></section><section className="paper mt-5 rounded-3xl p-6 sm:p-8"><div className="flex items-center justify-between"><div><p className="text-xs font-bold tracking-widest text-indigo-500">随机成语卡片</p><h2 className="display mt-3 text-3xl font-bold text-indigo-950">{random.title}</h2><Pronunciation items={random.keyPronunciations}/></div><span className="chip hidden bg-indigo-50 text-indigo-700 sm:inline-flex">{random.category}</span></div><p className="mt-5 max-w-3xl leading-8 text-slate-600">{random.meaning}</p><Link to={`/idioms/${random.id}`} className="mt-5 inline-flex items-center gap-1 text-sm font-bold text-indigo-700">查看详情 <ArrowRight size={16}/></Link></section></div>}
